# Retail-Data-Metrics
Scalable data pipeline that reads retail datasets from HDFS, computes key business metrics using PySpark SQL, and stores the processed outputs in a Google Cloud Storage (GCS) bucket.
